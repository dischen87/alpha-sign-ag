---
/**
 * Input Component
 * Form input with label, helper text, and error states.
 * Professional styling with proper focus states.
 */
interface Props {
  type?: 'text' | 'email' | 'password' | 'tel' | 'url' | 'number' | 'search';
  name: string;
  id?: string;
  label?: string;
  placeholder?: string;
  value?: string;
  required?: boolean;
  disabled?: boolean;
  readonly?: boolean;
  error?: string;
  hint?: string;
  autocomplete?: string;
  class?: string;
}

const {
  type = 'text',
  name,
  id = name,
  label,
  placeholder,
  value,
  required = false,
  disabled = false,
  readonly = false,
  error,
  hint,
  autocomplete,
  class: className = '',
} = Astro.props;

const inputStyles = `
  w-full
  px-4 py-3.5
  bg-background
  border-2 border-border
  rounded-[var(--radius)]
  text-foreground text-base leading-normal
  placeholder:text-muted-foreground
  transition-all duration-200 ease-out
  hover:border-muted-foreground/50
  focus:outline-none focus:border-primary focus:ring-4 focus:ring-primary/10
  disabled:opacity-50 disabled:cursor-not-allowed disabled:bg-muted disabled:hover:border-border
  read-only:bg-muted read-only:cursor-default read-only:hover:border-border
  dark:bg-input dark:border-border dark:hover:border-muted-foreground/30
  dark:focus:ring-primary/20
  min-h-[52px]
`;

const errorStyles = error
  ? 'border-destructive focus:border-destructive focus:ring-destructive/20'
  : '';

const classes = [inputStyles, errorStyles, className].filter(Boolean).join(' ');
---

<div class="space-y-2">
  {label && (
    <label for={id} class="block text-sm font-semibold text-foreground mb-1.5">
      {label}
      {required && <span class="text-destructive ml-0.5" aria-hidden="true">*</span>}
    </label>
  )}

  <div class="relative">
    <slot name="icon-left" />
    <input
      type={type}
      name={name}
      id={id}
      placeholder={placeholder}
      value={value}
      required={required}
      disabled={disabled}
      readonly={readonly}
      autocomplete={autocomplete}
      aria-invalid={error ? 'true' : undefined}
      aria-describedby={error ? `${id}-error` : hint ? `${id}-hint` : undefined}
      class={classes}
    />
    <slot name="icon-right" />
  </div>

  {hint && !error && (
    <p id={`${id}-hint`} class="text-sm text-muted-foreground">
      {hint}
    </p>
  )}

  {error && (
    <p id={`${id}-error`} class="text-sm text-destructive flex items-center gap-1.5" role="alert">
      <svg class="w-4 h-4 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
      </svg>
      {error}
    </p>
  )}
</div>
